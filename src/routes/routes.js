// import Listing from 'pages/Listing/Listing'
import Home from 'pages/Home/Home'

const PATHS = {
  HOME: '/',
};

const ROUTES = [
  {
    path: PATHS.HOME,
    component: Home
  }
];

export { ROUTES, PATHS }
