// import Listing from 'pages/Listing/Listing'
import Home from 'pages/Home/Home'

const PATHS = {
  LISTING: '/',
};

const ROUTES = [
  {
    path: PATHS.LISTING,
    component: Home
  }
];

export { ROUTES, PATHS }
